package WRedisPackage

import (
	"fmt"
)

func main() {
	/*	config.ConfigInit()
		RedisInit(config.Conf.Redis.Address,config.Conf.Redis.Password,0,0)
		GetInstance().RedisDial(5)
		defer GetInstance().Close()
	*/

	fmt.Println("redis test")

}
